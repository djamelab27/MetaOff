{
  "name": "metaoff",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-extension.mjs && node --check app.js && node --check popup.js && node --check worker.js && node --check src/image-metadata.js && node --check src/zip.js",
    "package": "node scripts/package.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
